<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: linear-gradient(to right, #e0f7fa, #b9fbc0); /* Light gradient background */
        }
        header, footer {
            background-color: #4a90e2; /* Light blue header/footer */
            color: white;
            text-align: center;
            padding: 1rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        main {
            display: flex;
            flex: 1;
            overflow: hidden;
        }
        nav {
            width: 240px;
            background-color: #e0f2f1; /* Light green sidebar */
            padding: 1rem;
            border-right: 1px solid #ddd;
            box-shadow: 2px 0 6px rgba(0, 0, 0, 0.1);
            transition: width 0.3s;
        }
        nav:hover {
            width: 260px; /* Expand sidebar on hover */
        }
        nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        nav ul li {
            margin: 0.5rem 0;
        }
        nav ul li a {
            text-decoration: none;
            color: #333;
            font-weight: bold;
            padding: 0.5rem;
            display: block;
            border-radius: 4px;
            transition: background-color 0.3s, color 0.3s;
        }
        nav ul li a:hover, nav ul li a.active {
            background-color: #b2dfdb; /* Hover and active color */
            color: #004d40;
        }
        section {
            flex: 1;
            padding: 2rem;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            margin: 1rem;
            transition: box-shadow 0.3s;
        }
        section:hover {
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
        }
        footer {
            background-color: #4a90e2;
            padding: 0.5rem;
            box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.2);
        }
        .footer-links {
            margin-top: 10px;
        }
        .footer-links a {
            color: #b9fbc0; /* Light green */
            text-decoration: none;
            margin: 0 10px;
        }
        .footer-links a:hover {
            text-decoration: underline;
        }
        .welcome {
            font-size: 24px;
            margin-bottom: 1rem;
            color: #333;
        }
        p {
            color: #555;
        }
        @media (max-width: 768px) {
            nav {
                width: 100%;
                border-right: none;
                box-shadow: none;
            }
            nav ul {
                display: flex;
                justify-content: space-around;
                padding: 0;
            }
            nav ul li {
                margin: 0;
            }
            nav ul li a {
                padding: 0.5rem 1rem;
            }
            section {
                margin: 0.5rem;
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <h1>Dashboard</h1>
    </header>

    <!-- Main content with Sidebar and Welcome Message -->
    <main>
        <!-- Sidebar -->
        <nav>
            <ul>
                <li><a href="#" class="active">Home</a></li>
                <li><a href="#">Profile</a></li>
                <li><a href="#">Settings</a></li>
                <li><a href="#">Logout</a></li>
            </ul>
        </nav>

        <!-- Main Section -->
        <section>
            <div class="welcome" id="welcomeMessage">Welcome, <%= account %></div>
            <p>This is your dashboard. Here you can manage your profile and settings.</p>
        </section>
    </main>

    <!-- Footer -->
    <footer>
        <p>© 2024 Your Company</p>
        <div class="footer-links">
            <a href="#">Privacy Policy</a>
            <a href="#">Terms of Service</a>
            <a href="#">Contact Us</a>
        </div>
    </footer>

    <script>
      // Uncomment if needed for dynamic user address
        /*
        const params = new URLSearchParams(window.location.search);
        const userAddress = params.get('address') || 'Guest';
        document.getElementById('welcomeMessage').textContent = `Welcome, ${userAddress}`;
      */
    </script>
</body>
</html>
